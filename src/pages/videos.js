import React from 'react';

import Page from '../components/page';

const PageConfig = {
	title: 'JoJo Videos'
};

const Photos = () => {
	return (
		<Page {...PageConfig}>
			Videos
		</Page>
	);
};

export default Photos;
